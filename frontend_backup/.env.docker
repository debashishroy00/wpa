# Docker environment configuration
# This file is used when running in Docker containers

# Backend API URL for Docker internal communication
VITE_API_BASE_URL=http://backend:8000
# External URL for browser requests (when needed)
VITE_API_BASE_URL_EXTERNAL=http://localhost:8000
VITE_ENVIRONMENT=docker

# Docker-specific settings
CHOKIDAR_USEPOLLING=true