# WealthPath AI - Emergency Minimal Requirements
# For deployment when memory is extremely limited (<256MB)

# Core API only
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Database essentials
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Data validation
pydantic==2.5.0
pydantic-settings==2.1.0

# External APIs (OpenAI, etc.)
openai==1.3.0
httpx==0.25.2

# Config
python-dotenv==1.0.0

# Cache
redis==5.0.1

# Minimal logging
structlog==23.2.0

# Note: Temporarily skip vector DB if causing memory issues
# Add back when memory allows:
# hnswlib==0.7.0