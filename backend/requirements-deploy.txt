# WealthPath AI - ULTRA-MINIMAL Deployment (NO ML/CUDA)
# Target: <256MB memory usage, NO GPU dependencies

# Core FastAPI Framework
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Database & ORM
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9

# Redis & Caching
redis==5.0.1

# Authentication & Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Data Validation
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP Client & External APIs
httpx==0.25.2

# LLM Providers (API only - no local models)
openai==1.3.0
# REMOVED google-generativeai - might pull in protobuf/grpc with CUDA
# REMOVED anthropic - might have tokenizers dependency

# Configuration
python-dotenv==1.0.0

# Minimal logging
structlog==23.2.0

# REMOVED ALL OF THESE - They can pull in CUDA:
# - hnswlib (requires numpy which can pull CUDA)
# - numpy (can pull in CUDA dependencies)
# - tiktoken (might have hidden dependencies)
# - google-generativeai (pulls in heavy Google deps)
# - anthropic (pulls in tokenizers which needs rust/heavy deps)